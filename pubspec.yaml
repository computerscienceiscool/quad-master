name: quad_master
description: "Four corners. Total control."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_local_notifications: ^16.3.0
  share_plus: ^7.2.1
  uuid: ^4.2.1
  timezone: ^0.9.2
  
  # New dependencies for enhancements
  flutter_slidable: ^3.0.0        # Swipe actions
  home_widget: ^0.4.0              # Home screen widgets (iOS/Android)
  quick_actions: ^1.0.0            # App icon shortcuts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  hive_generator: ^2.0.1
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
